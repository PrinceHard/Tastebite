import { Inter, Lobster} from 'next/font/google'

export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
})

export const lobster = Lobster({
    subsets: ['latin'],
    weight: ['400']
})
